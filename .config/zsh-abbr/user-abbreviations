abbr -g "git a"="git add"
abbr -g "git c"="git commit"
abbr -g "git cb"="git checkout -b"
abbr -g "git cm"="git commit -m \"%\""
abbr -g "git ga"="git add"
abbr -g "git gc"="git commit"
abbr -g "git gcb"="git checkout -b"
abbr -g "git gcm"="git commit -m \"%\""
abbr -g "git gco"="git checkout"
abbr -g "git gcom"="git checkout main"
abbr -g "git gl"="git pull"
abbr -g "git gp"="git push"
abbr -g "git gs"="git status"
abbr -g "git l"="git pull"
abbr -g "git o"="git checkout"
abbr -g "git om"="git checkout main"
abbr -g "git p"="git push"
abbr -g "git s"="git status"
abbr "a"="git add"
abbr "c"="git commit"
abbr "cb"="git checkout -b"
abbr "cm"="git commit -m \"%\""
abbr "ga"="git add"
abbr "gc"="git commit"
abbr "gcb"="git checkout -b"
abbr "gcm"="git commit -m \"%\""
abbr "gco"="git checkout"
abbr "gcom"="git checkout main"
abbr "gl"="git pull"
abbr "gp"="git push"
abbr "l"="git pull"
abbr "o"="git checkout"
abbr "om"="git checkout main"
abbr "p"="git push"
abbr "s"="git status"
